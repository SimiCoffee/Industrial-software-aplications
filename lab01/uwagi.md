- klasa z dużej litery
- nazwenictwo funkcji
- statyczna vs not metoda
- funkcje zwracają wartości, printy zrobić gdzie indziej (dla wyników)

1. Amstrong

   - argumenty, własny scanner (ważne przy testowaniu zwłaszcza)
   - dodaj wrapper? pomocniczą? readNubmer? - jak w Sequence

2. Seconds

   - dodać pomocniczą - jak w Sequence
   - planety dodać jako enumy
   - https://snipplr.com/view/42422/the-planet-enum-example

3. Sequence

   - string output
   - repeat zamiast for w for?
   - StringBuilder!

4. JAR file
   - zamiast `/bin` dać albo ./bin albo bin
   - brak uwag
   - tar i jar podobne (command wise)

---

1. Zrobić testy w głównej apce

   - Armstrong.isArmstrong(9) == true

2. Planety
   - `value` zamienić na bardziej czytelną zmienną
   - przenieść funkcjonalnosć do enuma
