- instead of using a a root path in code use `new ClassPathResource`
- with errors you can use 'System.err.println' instead of 'out'

- if there is no change of state, this. or soemthign you should add that that class is static
